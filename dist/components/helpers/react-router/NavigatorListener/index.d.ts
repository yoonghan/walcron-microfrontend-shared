import { ReactNode } from "react";
declare const NavigatorListener: ({ children, appName, containerName, }: {
    children: ReactNode;
    appName: string;
    containerName: string;
}) => ReactNode;
export default NavigatorListener;
